Description:
    Generate the config for Heracles

Example:
    rails generate heracles:wrapper:install -k <API_KEY_FOR_HERACLES>

    This will create:
        config/initializers/heracles-wrapper.rb
